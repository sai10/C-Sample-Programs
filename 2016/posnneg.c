#include<stdio.h>
void main()
{
    int n;
    printf("Enter the number:");
    scanf("%d",&n);
    if(n>0)
        printf("Number is positive");
    if(n<0)
        printf("Nember is negative");
}
